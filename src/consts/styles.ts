import variables from '@/assets/export.module.scss'
const { screenXL, screenLG, screenMD, screenSM } = variables

export const screenSizes = {
  sm: screenSM,
  md: screenMD,
  lg: screenLG,
  xl: screenXL
}
